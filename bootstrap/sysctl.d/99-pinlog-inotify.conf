# PinLog single-node k3s host: root UID currently consumes the legacy 128
# inotify-instance ceiling. 512 provides 4x the observed steady-state demand
# while leaving watch-count and global file-table limits unchanged.
fs.inotify.max_user_instances = 512
