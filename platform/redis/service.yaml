apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: pinlog-prod
  labels:
    app.kubernetes.io/name: redis
spec:
  ports:
    - port: 6379
      targetPort: redis
      name: redis
  selector:
    app.kubernetes.io/name: redis
