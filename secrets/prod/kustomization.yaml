apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - postgres-credentials.sealedsecret.yaml
  - ghcr-back-pull.sealedsecret.yaml
  - back-owner-secrets.sealedsecret.yaml
  - image-worker-credentials.sealedsecret.yaml
  # Reserved handoff artifact: intentionally absent until the token owner seals it.
  - cloudbeaver-cloudflared-token.sealedsecret.yaml
