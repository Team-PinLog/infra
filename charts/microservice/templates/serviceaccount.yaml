{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "microservice.serviceAccountName" . }}
  labels:
    {{- include "microservice.labels" . | nindent 4 }}
# 앱이 k8s API를 호출할 일이 없으므로 토큰을 마운트하지 않는다.
automountServiceAccountToken: false
{{- end }}
